// declaraciones de función
// Función con nombre
function suma1(a, b) {
  return a + b;
}

// ========================

// expresiones de función


// Función con nombre
const suma3 = function suma3(a, b) {
  return a + b;
};

// Función anónima
const suma2 = function (a, b) {
  return a + b;
};

// Función de flecha (anónima por defecto)
const suma4 = (a, b) => {
  return a + b;
};
